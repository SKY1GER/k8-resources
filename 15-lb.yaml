apiVersion: v1
kind: Service
metadata:
  name: service-loadbalancer
spec:
  type: LoadBalancer
  selector:
    name: frontend
    environment: dev
    project: expense
    component: frontend
  ports:
  - protocol: tcp
    port: 80
    targetPort: 32760
